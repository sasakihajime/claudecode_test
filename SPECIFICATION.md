# Todoアプリケーション 仕様書

## 1. 概要

### 1.1 目的
ユーザーが日常のタスクを管理できるシンプルで直感的なTodoアプリケーションを提供する。

### 1.2 対象ユーザー
- 個人でタスク管理を行いたいユーザー
- シンプルなインターフェースを求めるユーザー
- ブラウザベースのツールを好むユーザー

### 1.3 スコープ
- シングルユーザー向けのローカルストレージベースのアプリケーション
- インターネット接続不要で動作
- バックエンドサーバー不要

## 2. 機能要件

### 2.1 タスク管理機能

#### 2.1.1 タスク追加
- **要件ID**: FR-001
- **説明**: ユーザーが新しいタスクを追加できる
- **詳細**:
  - テキスト入力フィールドにタスク内容を入力
  - 「追加」ボタンのクリックまたはEnterキーで追加
  - 空のタスクは追加できない（バリデーション）
  - 追加後、入力フィールドは自動的にクリアされる

#### 2.1.2 タスク完了/未完了の切り替え
- **要件ID**: FR-002
- **説明**: タスクの完了状態を切り替えられる
- **詳細**:
  - チェックボックスをクリックして完了/未完了を切り替え
  - 完了タスクは視覚的に区別される（取り消し線など）
  - 状態変更は即座にlocalStorageに保存される

#### 2.1.3 タスク削除
- **要件ID**: FR-003
- **説明**: 個別のタスクを削除できる
- **詳細**:
  - 各タスクに「削除」ボタンを配置
  - クリックで即座に削除（確認ダイアログなし）
  - 削除後は自動的にリストが更新される

#### 2.1.4 完了済みタスクの一括削除
- **要件ID**: FR-004
- **説明**: 完了したタスクを一括で削除できる
- **詳細**:
  - 「完了済みを削除」ボタンを提供
  - 完了済みタスクが存在する場合のみボタンを表示
  - クリックで全ての完了済みタスクを削除

### 2.2 フィルタリング機能

#### 2.2.1 表示フィルター
- **要件ID**: FR-005
- **説明**: タスクの表示を状態別にフィルタリングできる
- **フィルター種別**:
  - **すべて**: 全てのタスクを表示（デフォルト）
  - **未完了**: 未完了のタスクのみ表示
  - **完了済み**: 完了済みのタスクのみ表示
- **詳細**:
  - フィルターボタンで切り替え
  - アクティブなフィルターは視覚的に強調表示
  - フィルター変更時も実際のデータは削除されない

### 2.3 統計表示機能

#### 2.3.1 未完了タスクカウント
- **要件ID**: FR-006
- **説明**: 未完了タスクの数を表示する
- **詳細**:
  - 「X 個のタスク」の形式で表示
  - タスクの追加・削除・完了時に自動更新

### 2.4 データ永続化機能

#### 2.4.1 ローカルストレージへの保存
- **要件ID**: FR-007
- **説明**: タスクデータをブラウザのlocalStorageに保存する
- **詳細**:
  - タスクの追加・更新・削除時に自動保存
  - JSON形式でシリアライズして保存
  - ブラウザを閉じても データが保持される

#### 2.4.2 データの読み込み
- **要件ID**: FR-008
- **説明**: アプリ起動時にlocalStorageからデータを読み込む
- **詳細**:
  - DOMContentLoadedイベント時に初期化
  - 保存データがない場合は空の配列で初期化
  - パースエラー時も安全に初期化される

## 3. データモデル

### 3.1 Todoオブジェクト
```javascript
{
  id: Number,           // タイムスタンプベースのユニークID
  text: String,         // タスクの内容
  completed: Boolean,   // 完了状態（true: 完了, false: 未完了）
  createdAt: String     // 作成日時（ISO 8601形式）
}
```

### 3.2 データ構造
- **ストレージキー**: `"todos"`
- **データ形式**: JSON配列
- **例**:
```json
[
  {
    "id": 1705123456789,
    "text": "買い物に行く",
    "completed": false,
    "createdAt": "2024-01-13T12:30:56.789Z"
  },
  {
    "id": 1705123467890,
    "text": "レポートを書く",
    "completed": true,
    "createdAt": "2024-01-13T12:31:07.890Z"
  }
]
```

## 4. 非機能要件

### 4.1 パフォーマンス
- **要件ID**: NFR-001
- タスクの追加・削除・更新は即座に反映される（100ms以内）
- 1000件のタスクでも快適に動作する

### 4.2 ユーザビリティ
- **要件ID**: NFR-002
- 直感的で分かりやすいUI
- 日本語でのメッセージ表示
- キーボード操作のサポート（Enter キーでタスク追加）

### 4.3 互換性
- **要件ID**: NFR-003
- モダンブラウザでの動作保証
  - Google Chrome（最新版）
  - Mozilla Firefox（最新版）
  - Safari（最新版）
  - Microsoft Edge（最新版）
- localStorage API をサポートするブラウザ

### 4.4 レスポンシブデザイン
- **要件ID**: NFR-004
- デスクトップ、タブレット、スマートフォンでの表示に対応
- モバイルでも操作しやすいUI

### 4.5 アクセシビリティ
- **要件ID**: NFR-005
- セマンティックなHTML構造
- 適切なコントラスト比
- キーボードナビゲーション対応

## 5. 技術仕様

### 5.1 技術スタック
- **フロントエンド**: Vanilla JavaScript (ES6+)
- **マークアップ**: HTML5
- **スタイリング**: CSS3
- **データストレージ**: localStorage API

### 5.2 ファイル構成
```
/
├── index.html         # メインHTMLファイル
├── style.css          # スタイルシート
├── app.js             # アプリケーションロジック
├── README.md          # プロジェクト説明
└── SPECIFICATION.md   # 本仕様書
```

### 5.3 主要クラス・メソッド

#### TodoApp クラス
- **constructor()**: 初期化処理
- **initElements()**: DOM要素の取得
- **attachEventListeners()**: イベントリスナーの設定
- **addTodo()**: タスク追加
- **toggleTodo(id)**: タスクの完了状態切り替え
- **deleteTodo(id)**: タスク削除
- **clearCompletedTodos()**: 完了済みタスク一括削除
- **setFilter(filter)**: フィルター設定
- **getFilteredTodos()**: フィルタリングされたタスク取得
- **render()**: UI再描画
- **createTodoElement(todo)**: Todo要素の生成
- **updateStats()**: 統計情報更新
- **saveTodos()**: localStorageへ保存
- **loadTodos()**: localStorageから読み込み

## 6. ユーザーインターフェース仕様

### 6.1 レイアウト構成
1. **ヘッダー部**
   - アプリケーションタイトル

2. **入力部**
   - テキスト入力フィールド
   - 追加ボタン

3. **フィルター部**
   - すべて / 未完了 / 完了済み ボタン

4. **タスクリスト部**
   - タスク一覧表示エリア
   - 空の状態メッセージ

5. **フッター部**
   - 未完了タスク数表示
   - 完了済みを削除ボタン

### 6.2 タスクアイテムのUI
各タスクアイテムは以下の要素で構成:
- チェックボックス（完了状態の切り替え）
- タスクテキスト（完了時は取り消し線）
- 削除ボタン

### 6.3 状態別メッセージ
- **全タスク表示時（タスクなし）**: "タスクがありません"
- **未完了フィルター時（該当なし）**: "未完了のタスクはありません"
- **完了済みフィルター時（該当なし）**: "完了済みのタスクはありません"

## 7. エラー処理

### 7.1 入力バリデーション
- 空のタスクは追加できない（trim()後の文字列長チェック）
- エラー時は何も実行せずにreturn

### 7.2 データ読み込みエラー
- localStorageのデータが破損している場合は空配列で初期化
- JSON.parse()エラーは例外処理で対応

## 8. 制約事項

### 8.1 技術的制約
- localStorage容量制限（ブラウザ依存、通常5-10MB）
- 単一ユーザーのみ対応（マルチユーザー機能なし）
- オフラインのみ動作（サーバー同期なし）
- 端末間でのデータ同期不可

### 8.2 機能的制約
- タスクの編集機能なし（削除して再追加が必要）
- タスクの並べ替え機能なし
- カテゴリーやタグ機能なし
- 期限設定機能なし
- 優先度設定機能なし

## 9. 将来的な拡張候補

### 9.1 Phase 2 候補機能
- [ ] タスク編集機能
- [ ] タスクのドラッグ&ドロップによる並べ替え
- [ ] タスクの検索機能
- [ ] カテゴリー/タグ機能
- [ ] 期限設定と通知機能

### 9.2 Phase 3 候補機能
- [ ] 複数デバイス間でのデータ同期（クラウド連携）
- [ ] ユーザー認証機能
- [ ] タスクの共有機能
- [ ] インポート/エクスポート機能（JSON, CSV）
- [ ] テーマ切り替え（ダークモード等）

## 10. テスト要件

### 10.1 機能テスト
- [ ] タスク追加が正しく動作する
- [ ] タスク完了の切り替えが正しく動作する
- [ ] タスク削除が正しく動作する
- [ ] 完了済み一括削除が正しく動作する
- [ ] フィルター機能が正しく動作する
- [ ] localStorageへの保存・読み込みが正しく動作する

### 10.2 エッジケーステスト
- [ ] 空のタスクを追加しようとした場合
- [ ] 大量のタスク（1000件以上）を扱う場合
- [ ] localStorage が利用できない環境での動作
- [ ] 特殊文字を含むタスクの処理

### 10.3 ブラウザテスト
- [ ] Chrome での動作確認
- [ ] Firefox での動作確認
- [ ] Safari での動作確認
- [ ] Edge での動作確認
- [ ] モバイルブラウザでの動作確認

## 11. 変更履歴

| バージョン | 日付 | 変更内容 | 作成者 |
|-----------|------|---------|--------|
| 1.0.0 | 2026-01-14 | 初版作成 | - |

---

**文書管理情報**
- 文書名: Todoアプリケーション 仕様書
- 文書ID: SPEC-TODO-001
- 最終更新日: 2026-01-14
